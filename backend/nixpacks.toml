# Nixpacks configuration for Railway deployment
[phases.setup]
nixPkgs = ["nodejs-18_x", "npm-9_x"]

[phases.install]
cmds = [
  "npm ci --omit=dev",
  "npm run db:generate"
]

[phases.build]
cmds = [
  "npm run build"
]

[start]
cmd = "npm start"
